# Neon DB connection string
# Used by drizzle-kit CLI (db:generate, db:migrate, db:push, db:studio)
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require

# Same value — Nuxt reads this and maps it to runtimeConfig.databaseUrl
NUXT_DATABASE_URL=postgres://user:password@host/dbname?sslmode=require

# Clerk — https://dashboard.clerk.com → API Keys
NUXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
NUXT_CLERK_SECRET_KEY=sk_test_...

# Google OAuth — https://console.cloud.google.com → APIs & Services → Credentials
# Enable YouTube Data API v3; add /api/auth/youtube/callback as an authorized redirect URI
NUXT_GOOGLE_CLIENT_ID=
NUXT_GOOGLE_CLIENT_SECRET=

# Algolia — https://dashboard.algolia.com → Settings → API Keys
# App ID: found on the API Keys page
# Admin API Key: used server-side only for indexing — never expose to the browser
# Search-Only API Key: safe to expose to the browser for read-only queries
# Index Name: the index that stores feed_items (default: feed_items)
NUXT_ALGOLIA_APP_ID=
NUXT_ALGOLIA_ADMIN_API_KEY=
NUXT_PUBLIC_ALGOLIA_APP_ID=
NUXT_PUBLIC_ALGOLIA_SEARCH_API_KEY=
NUXT_PUBLIC_ALGOLIA_INDEX_NAME=feed_items
